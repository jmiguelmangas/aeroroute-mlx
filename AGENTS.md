# AeroRoute MLX rules

Never calculate or rank routes. Reject unsupported contract/model manifests;
validate generated numeric claims against supplied facts; normal CI must work
without MLX, model downloads, or Apple Silicon.
